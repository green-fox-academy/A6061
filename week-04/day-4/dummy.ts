'use strict';

export function addNum(a: number, b: number): number {
  return a + b;
}

export function subtractNum(a: number, b: number): number {
  return a - b;
};

// addNum(1,2);
// subtractNum(2,1);