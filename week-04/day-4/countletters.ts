'use strict';

export function letterCount(letter: string) {
  
  let string1: string = letter.split("").push(stringArr);
  let stringArr: string [] = []
}



// Write a function,
// that takes a string as an argument and returns a dictionary with all letters in the string as keys,
// and numbers as values that shows how many occurrences there are.